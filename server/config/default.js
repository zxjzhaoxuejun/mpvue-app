const config = {
  // 启动端口
  port: 8000,
  // 数据库配置
  database: {
    DATABASE: 'cauth',
    USERNAME: 'root',
    PASSWORD: 'z13203073508',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config
